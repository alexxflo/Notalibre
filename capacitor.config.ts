import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vortex.social',
  appName: 'VORTEX',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
