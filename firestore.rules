rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Esto cubre 'messages', 'posts' y cualquier otra colección
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
