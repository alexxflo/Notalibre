rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // El comodín {document=**} asegura TODAS las colecciones y subcolecciones
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
