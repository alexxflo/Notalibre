rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Esta regla permite leer y escribir a CUALQUIER usuario que haya iniciado sesión
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
